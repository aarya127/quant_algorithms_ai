# ─────────────────────────────────────────────────────────────────────────────
# IMPORTANT: This file is a template.
# Replace every "your-*" placeholder with a real value before applying.
#
# To create the secret from the command line instead of editing this file:
#
#   kubectl create secret generic invest-ai-secrets \
#     --namespace invest-ai \
#     --from-literal=FINNHUB_API_KEY=<key> \
#     --from-literal=ALPHAVANTAGE_API_KEY=<key> \
#     --from-literal=ALPACA_API_KEY=<key> \
#     --from-literal=ALPACA_SECRET_KEY=<secret> \
#     --from-literal=TWITTER_API_KEY=<key> \
#     --from-literal=TWITTER_API_SECRET=<secret> \
#     --from-literal=TWITTER_BEARER_TOKEN=<token> \
#     --from-literal=NVIDIA_API_KEY=<key> \
#     --from-literal=CHART_IMG_KEY=<key>
#
# DO NOT commit real keys to version control.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: invest-ai-secrets
  namespace: invest-ai
  labels:
    app.kubernetes.io/name: invest-ai
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  FINNHUB_API_KEY: "your-finnhub-key"
  ALPHAVANTAGE_API_KEY: "your-alphavantage-key"
  ALPACA_API_KEY: "your-alpaca-key"
  ALPACA_SECRET_KEY: "your-alpaca-secret"
  TWITTER_API_KEY: "your-twitter-key"
  TWITTER_API_SECRET: "your-twitter-secret"
  TWITTER_BEARER_TOKEN: "your-twitter-bearer-token"
  NVIDIA_API_KEY: "your-nvidia-key"
  CHART_IMG_KEY: "your-chartimg-key"
