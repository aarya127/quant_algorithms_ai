Flask==3.0.0
gunicorn==21.2.0
pandas==2.1.4
yfinance>=0.2.36
transformers==4.36.2
# torch is installed separately in Dockerfile with the CPU-only wheel
finnhub-python==2.4.19
requests==2.31.0
tweepy==4.16.0
websockets==15.0.1
scipy>=1.11.0
cachetools>=5.3.0

# ML runtime — REQUIRED to unpickle/serve models from the registry (predictor.py)
# and to run the in-container retrain steps. Version bounds MUST match
# requirements-pipeline.txt so models trained there load here without errors.
numpy>=1.26.0
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
joblib>=1.3.0
mlflow>=2.0.0,<3.0.0

# LLM providers for the analyst brief (llm_router: NVIDIA NIM + OpenAI use this
# client; anthropic optional via ANTHROPIC_API_KEY)
openai>=1.30.0
